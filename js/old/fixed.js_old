$(function () {
  $(window).scroll(function () {

      if ($(this).scrollTop() > '800') {
          $('#menu_block').addClass('fixed');
      } else {
          $('#menu_block').removeClass('fixed');
      }
  });
});
  

